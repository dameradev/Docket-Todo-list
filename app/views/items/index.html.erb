<h1>Woop!</h1>

<% @items.each do |item| %>
  <h2><%= link_to (item.id).to_s + (". ") + (item.title), item %></h2>
  <p><%= item.description %></p>
<% end %>

<%= link_to 'New item', new_item_path %>
