
<% if user_signed_in? %>
  <% @items.each_with_index do |item, i| %>
    <h2><%= link_to (i+1).to_s + (". ") + (item.title), item %></h2>
    <p><%= item.description %></p>
  <% end %>
<% else %>
  <%= render 'welcome_page'%>
<% end %>
